1. boolean istGekauft überflüssig? weil wenn besitzerId != null dann ist es gekauft. // has been implemented alongside boolean; -> remove bulk!

2. Bahnhof miete ist nicht in der DB, sondern wird berechnet. (siehe Suedbahnhof.java)/clashes with previous design

